import styled from 'styled-components';

const H2 = styled.h2`
  text-transform: uppercase;
  text-align: center;
  font-size: 4rem;
  letter-spacing: 0.55rem;
  font-weight: 300;
`;

export default H2;
